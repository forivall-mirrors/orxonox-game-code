PROJECT(Orxonox)

AUX_SOURCE_DIRECTORY(tinyxml TINYXML_SRC_FILES)

SET (UTIL_SRC_FILES
  ${TINYXML_SRC_FILES}
  substring.cc
)

IF(WIN32)
  ADD_LIBRARY(${Orxonox_BINARY_DIR}/util ${UTIL_SRC_FILES} )
ELSE(WIN32)
  ADD_LIBRARY(${Orxonox_BINARY_DIR}/util SHARED ${UTIL_SRC_FILES} )
ENDIF(WIN32)
SET_TARGET_PROPERTIES( ${Orxonox_BINARY_DIR}/util PROPERTIES LINK_FLAGS "--no-undefined" )


IF(TESTING_ENABLED)
  ADD_SUBDIRECTORY(testing)
ENDIF(TESTING_ENABLED)
